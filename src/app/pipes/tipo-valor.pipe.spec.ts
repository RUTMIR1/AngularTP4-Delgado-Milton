import { TipoValorPipe } from './tipo-valor.pipe';

describe('TipoValorPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoValorPipe();
    expect(pipe).toBeTruthy();
  });
});
